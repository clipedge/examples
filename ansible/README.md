# ansible

```
#:~/ansible/minikube$ mkdir -p roles/common/{tasks,handlers,templates,files,vars,defaults,meta,library,module_utils,lookup_plugins}
```

verify directories added

```
phil@phil-Lenovo-IdeaPad-S145-15IWL:~/ansible/minikube$ ls roles/common/
defaults  handlers  lookup_plugins  module_utils  templates
files     library   meta            tasks         vars
```
